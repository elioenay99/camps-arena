# Tasks — add-ligas-piramide

Checklist faseado (Fase 0 → 5, igual à proposal). Gates por fase: typecheck / lint / test / build verdes; DDL aplicada via MCP mostrando o SQL ANTES ([[feedback-mcp-autonomia]] + REGRA 4); validação ao vivo mobile-first (390px) nos 2 temas (Dracula/Canarinho). Tudo aditivo/nullable — zero regressão nos torneios legados.

## Fase 0 — DDL fundacional + motor parametrizável

### 0.1 DDL (via MCP; espelhar em `supabase/schema.sql`)
- [x] 0.1.1 Pré-check read-only (mostrar SQL antes): contar `tournament_slots` (todos com clube/rótulo XOR), confirmar 0 torneios com `desempate_criterio` faltando (coluna nova), validar que nenhum `tournaments.formato='liga'` já está vinculado a pirâmide.
- [x] 0.1.2 Criar os 4 enums: `league_competition_status`, `league_season_status`, `league_ranking_base`, `league_boundary_mode` (bloco `do $$ ... if not exists ... end$$`, idempotente).
- [x] 0.1.3 Criar `league_competitions` (PK uuid, `created_by` set null, `status` default 'ativa', `desempate_padrao` default 'cbf' + CHECK `in ('cbf','ingles','custom')` — SEM 'espanhol' na Fase 0, `is_public` boolean default true, `nome` não-vazio + índice `created_by`).
- [x] 0.1.4 Criar `league_seasons` (`competition_id` cascade, `numero` >= 1, `status` default 'rascunho', `config_snapshot` jsonb, `previous_season_id` set null, `encerrada_em`; índice único `(competition_id, numero)` + índice de competição).
- [x] 0.1.5 Criar `league_division_seasons` (`season_id` cascade, `nivel` >= 1, `nome`, `tournament_id` restrict nullable, `por_nome` default false, `desempate` default 'cbf' + CHECK `in ('cbf','ingles','custom')`, `tamanho` 2..20; índices únicos `(season_id, nivel)` e `tournament_id` parcial + índice de season). A UNIQUE `(season_id, nivel)` é a SENTINELA de idempotência da montagem (criada ANTES dos tournaments).
- [x] 0.1.6 Criar `league_boundaries` (`season_id` cascade, `nivel_superior` >= 1, `vagas_rebaixamento`/`vagas_acesso` >= 0 default 0, `modo` default 'direto', `playoff_vagas` + CHECK de coerência por modo; índice único `(season_id, nivel_superior)` + índice de season).
- [x] 0.1.7 Criar `league_competitors` (`competition_id` cascade, `team_id` restrict, `rotulo`, `holder_user_id` set null NULLABLE = técnico que acompanha ou null = vaga gerida pelo dono; CHECK XOR clube/rótulo + não-vazio; índices únicos parciais team/rótulo case-insensitive + índice de competição).
- [x] 0.1.8 Criar `league_division_entries` (`division_season_id` cascade, `competitor_id` cascade, `slot_id` restrict nullable, `posicao_final`/`destino`/`resolvido_por`/`pontos`/`jogos`; CHECK `destino in ('sobe','cai','permanece')` e CHECK `resolvido_por in ('classificacao','playoff','sorteio','override')` — 'sorteio' é MOTIVO, não destino; índices únicos `(division_season_id, competitor_id)` e `slot_id` parcial + índices competidor/divisão).
- [x] 0.1.9 `alter table tournament_slots add column competitor_id uuid references league_competitors(id) on delete set null` + índice parcial. NÃO mexer no CHECK `slots_clube_xor_rotulo` existente.
- [x] 0.1.10 `alter table tournaments add column desempate_criterio text not null default 'cbf'` + CHECK `tournaments_desempate_valido in ('cbf','ingles','custom')` — SEM 'espanhol' na Fase 0 (alargado na Fase 5).
- [x] 0.1.11 RLS das 6 tabelas (SELECT pirâmide ativa/dono; INSERT/UPDATE/DELETE só dono via subquery transitiva) + helper `eh_dono_competition(uuid) security definer`. `enable row level security` nas 6 tabelas. As policies `tournaments_insert_owner`/`slots_insert_owner_rascunho` NÃO são relaxadas (o pré-preenchimento de slots roda pela RPC `montar_temporada`).
- [x] 0.1.12 RPC `montar_temporada(p_season_id uuid)` (SECURITY DEFINER, `set search_path = ''`, espelha o estilo de `aceitar_convite_vaga`): (1) valida `auth.uid() = league_competitions.created_by` da season senão `raise 'NAO_DONO'`; (2) idempotente pela sentinela `league_division_seasons.tournament_id`; (3) cria `tournaments(formato='liga', status='rascunho', por_nome, desempate_criterio, is_public herdado)`; (4) insere `tournament_slots` preenchidos (por nome: `rotulo`+`competitor_id`; por clube: `team_id`+`competitor_id`+`user_id`=`holder_user_id` COM degradação para NULL se colidir com `slots_um_clube_por_tecnico`); (5) cria `league_division_entries`. `revoke execute ... from public, anon; grant execute ... to authenticated`.
- [x] 0.1.13 Triggers de lock (security definer, bypass service_role): `lock_league_season` (encerrada imutável), `lock_league_division_season` (tournament_id/nivel/por_nome/tamanho travados fora de rascunho), `lock_league_competitor_identity` (team_id/rotulo travados após primeira entry; `holder_user_id` mutável), `lock_division_tournament_reopen` (em `tournaments`: barra status 'encerrado'→'ativo'/'rascunho' quando o torneio é divisão de season `em_fluxo`/`encerrada` — fecha o furo do `reabrirTorneio`). Estender `lock_slot_relations` para barrar mudança de `competitor_id` fora de rascunho.
- [x] 0.1.14 Aplicar via MCP (mostrar SQL completo antes); preferir BRANCH Supabase para o esquema grande, depois mesclar. Verificar colunas/constraints/índices/policies/RPC/triggers presentes.
- [x] 0.1.15 Espelhar TODA a DDL em `supabase/schema.sql` (mesmo estilo/comentários do arquivo).

### 0.2 Tipos
- [x] 0.2.1 Regenerar `src/lib/supabase/database.types.ts` (6 tabelas + enums + `tournament_slots.competitor_id` + `tournaments.desempate_criterio`).
- [x] 0.2.2 Typecheck verde (nenhum consumidor existente quebra — campos novos opcionais/nullable).

### 0.3 Motor de desempate parametrizável
- [x] 0.3.1 Em `computeStandings.ts`: exportar `TiebreakerPreset = 'cbf'|'ingles'|'custom'` (SEM 'espanhol' — Fase 5) e `TiebreakerSpec`; adicionar 3º param `tiebreaker: TiebreakerPreset = 'cbf'` em `computeStandings` (linha 78).
- [x] 0.3.2 Implementar `obterTiebreakerSpec(preset)`: `cbf` retorna `[pontos, vitorias, saldo, golsPro]` + `confrontoDiretoApenasEm2: true` (BYTE-IDÊNTICO ao atual); `ingles` `[pontos, saldo, golsPro, vitorias]` + `confrontoDiretoApenasEm2: true`. `custom`/`espanhol` NÃO entram na Fase 0.
- [x] 0.3.3 Refatorar a ordenação (linhas 148-155), o agrupamento de empatados (158-169) e o confronto direto (195-209) para usarem a `spec` — mantendo `porId` como tiebreaker final.
- [x] 0.3.4 Propagação no fetcher (SEM as TRÊS edições juntas o preset é IGNORADO silenciosamente): (a) incluir `desempate_criterio` na string `.select(...)` de `tournaments` em `getTournamentClassificacao.ts:~297`; (b) adicionar `desempate_criterio: string` à interface `TorneioClassificacao` (`getTournamentClassificacao.ts:16-32`); (c) passar `const desempate = (torneio.desempate_criterio as TiebreakerPreset) ?? 'cbf'` como 3º arg `tiebreaker` nos 3 call-sites do motor (linhas 404, 418, 591); param opcional `tiebreakerOverride?` para testes.
- [x] 0.3.5 Testes: preservar 100% de `computeStandings.test.ts` (default `cbf` passa intacto); adicionar `describe('presets de desempate')` com fixtures `ingles` (sem `espanhol` na Fase 0).

### 0.4 Gate Fase 0
- [x] 0.4.1 `pnpm typecheck && pnpm lint && pnpm test && pnpm build` verdes.
- [x] 0.4.2 Não-regressão do cbf coberta pelos 25 testes legados de `computeStandings` (default intocado) + 861 da suíte verdes; smoke de liga real ao vivo acontece na 1.4.2 (cria liga ponta-a-ponta).
- [x] 0.4.3 Revisão adversarial: DDL (4 lentes — RLS/RPC/triggers/Postgres; achados HIGH+MEDIUM corrigidos ANTES de aplicar) + motor (não-regressão byte-a-byte confirmada; nit L-1 da fase de grupos registrado como dívida no código). Presets sem 'espanhol' confirmados.

## Fase 1 — Fundação ponta-a-ponta

### 1.1 Schema de criação (Zod)
- [x] 1.1.1 `src/schema/leaguePyramidSchema.ts`: `createCompetitionSchema` (nome, `isPublic`, lista de divisões com `{ nivel, nome, porNome, desempate, tamanho, clubes|nomes }`, fronteiras `{ nivelSuperior, vagasAcesso, vagasRebaixamento, modo }`), presets Brasileirão 4-4 / Premier 3-3 / Personalizado. Refines (REJEITAM, não só avisam): níveis contínuos 1..N; fronteiras só entre adjacentes; tamanhos 2..20; `desempate in ('cbf','ingles')` (Fase 0/1); INVARIANTE de fechamento de tamanho por divisão `tamanho = tamanho - sobe - cai + recebidos_cima + recebidos_baixo` resultando em `[2,20]` em TODAS as divisões; pontas (divisão 1 não recebe de cima nem sobe; última não recebe de baixo nem cai); N=1 permitido (sem fronteiras).

### 1.2 Actions
- [x] 1.2.1 `src/actions/leaguePyramid.ts` → `createCompetition`: valida form, cria `league_competitions` (com `is_public`) + `league_seasons(numero=1, status='rascunho')` + `league_division_seasons` + `league_boundaries` + `league_competitors` (um por clube/nome, com `holder_user_id` opcional). Posse = `created_by`.
- [x] 1.2.2 `montarTemporada(seasonId)`: action thin que CHAMA a RPC `montar_temporada(seasonId)` (SECURITY DEFINER — pré-preenche `user_id` por dentro do definer; ver 0.1.12). A RPC cria os `tournaments(formato='liga', status='rascunho', por_nome, desempate_criterio, is_public herdado)`, insere `tournament_slots` preenchidos (clube via `team_id`+`competitor_id`+`user_id`=`holder_user_id` COM degradação para NULL na colisão `slots_um_clube_por_tecnico`; nome via `rotulo`+`competitor_id`), cria `league_division_entries`, atribui `tournament_id`. Idempotente pela sentinela `league_division_seasons.tournament_id`.
- [x] 1.2.3 `iniciarDivisao(divisionSeasonId)`: thin wrapper que chama `iniciarTorneio(tournament_id)` (reúso total) + transição `season → 'ativa'` quando todas iniciadas.
- [x] 1.2.4 `calcularFluxoTemporada(seasonId)` (read-only): lê `getTournamentClassificacao` por divisão; deriva base `posicao`/`ppg`; aplica fronteiras `'direto'` simétricas (N últimos caem / N primeiros sobem); sorteio crypto no empate exato da zona de corte (ordena TODOS os empatados na fronteira, preenche as vagas na ordem sorteada; semente gravada). Marca `resolvido_por='sorteio'` quando o corte foi por sorteio. Retorna PLANO (sem escrita).
- [x] 1.2.5 `confirmarFluxoTemporada(seasonId, ajustes?)` (escrita, idempotente): `season → 'em_fluxo'`; persiste `league_division_entries` (posicao_final/destino/`resolvido_por`/pontos/jogos); aplica override do dono (`resolvido_por='override'`); chama `montarProximaTemporada`; `season → 'encerrada'`.
- [x] 1.2.6 `montarProximaTemporada(seasonId)`: cria `league_seasons(numero=N+1, previous_season_id=N)`; realoca competidores ao destino (técnico `holder_user_id` acompanha); VALIDA o fechamento de tamanho por divisão e REJEITA (erro explícito, sem escrita) se alguma divisão sair de `[2,20]`; cria as `league_division_seasons` da N+1 ANTES dos tournaments (sentinela), depois chama a RPC `montar_temporada(N+1)` para os torneios/slots/entries. Idempotência: `league_seasons_numero_unico` (temporada) + sentinela `tournament_id` (montagem); re-rodar após falha parcial completa só o que faltou.
- [x] 1.2.7 Guard em `reabrirTorneio` (`src/actions/tournaments.ts:479`, camada (a) do freeze): após carregar o torneio, rejeitar se ele for uma divisão (`league_division_seasons.tournament_id = id`) cuja `league_seasons.status in ('em_fluxo','encerrada')` — retorna o erro de propriedade existente, sem reabrir. A defesa REAL é o trigger `lock_division_tournament_reopen` (0.1.13); este guard é a camada de UX.

### 1.3 UI (mobile-first 390px, 2 temas)
- [x] 1.3.1 Rota `src/app/dashboard/ligas/` (lista de pirâmides do dono) + `nova/` (wizard com presets).
- [x] 1.3.2 `LeagueWizard` (client leaf): passos preset → divisões → fronteiras → competidores (toggle nome/clube por divisão; chips para nomes, `TeamSearchInput` para clubes — reúso). Hidden fields tipados.
- [x] 1.3.3 Página da temporada `dashboard/ligas/[id]/` (RSC): cards por divisão, cada uma linkando ao torneio EXISTENTE. CORREÇÃO da validação ao vivo: usa `naoMontada` (toda divisão sem tournament_id), não o status da season (que fica 'rascunho' após montar). Trata `em_fluxo` (retomada).
- [x] 1.3.4 `StandingsTable`: prop opcional `zonas?: { acesso; rebaixamento }` (destaque de zona sobe/cai + legenda). Default vazio preserva uso standalone.
- [x] 1.3.5 `FluxoTemporadaPanel`: render do PLANO (sobe/cai/sorteio) com 2 cliques + ajuste manual do empate (TODOS os grupos de sorteio do nível, não só o primeiro).
- [x] 1.3.6 Fetchers RSC: `getCompetitions`, `getSeason`, `getDivisionStandings` (reúsa `getTournamentClassificacao` por divisão).

### 1.4 Gate Fase 1
- [x] 1.4.1 `pnpm typecheck && pnpm lint && pnpm test && pnpm build` verdes (912 testes). Casos (a)-(f) cobertos em `leaguePyramid.test.ts` + caso de sorteio-espúrio (igualdade exata no corte) e `leaguePyramidSchema.test.ts` ganhou o caso `sobe+cai > tamanho`. ACHADO: o gate de build estava MASCARADO por um `echo` no pipe — `leaguePyramid.ts` ("use server") exportava helpers síncronos; motor extraído para `src/features/league/flowEngine.ts`.
- [x] 1.4.2 Validação AO VIVO (chrome-devtools, logado, 390px), DUAS pirâmides ponta-a-ponta: (A) caminho feliz — criar → montar (RPC) → iniciar → placares (W.O.) → fluxo → confirmar → N+1 com realocação correta + `reabrirTorneio` de season encerrada BARRADO. (B) override de empate — placares com empate na zona de corte → grupos de sorteio reordenáveis → override do dono → N+1 conferida no banco (`resolvido_por` = sorteio/override/classificacao corretos). 4 bugs achados ao vivo e corrigidos: (1) build mascarado por `echo` no pipe — `leaguePyramid.ts` "use server" exportava helpers síncronos (motor extraído p/ `flowEngine.ts`); (2) página presa pós-montagem (usava status; trocado p/ `naoMontada`=toda divisão sem tournament_id); (3) `montarProximaTemporada` NÃO copiava as fronteiras (N+1 perdia sobe/cai — passo 1.5 idempotente); (4) ordem inicial do grupo de sorteio gerava "Ajuste fantasma" (vencedores primeiro).
- [x] 1.4.3 TRÊS rodadas de revisão adversarial por workflow (lentes paralelas + verificação cética + síntese): R1 = 18 achados (BLOQUEADO_HIGH), R2 = override de empate morto (B1/B2), R3 = dead-end por sobreposição de cortes na divisão do meio (HIGH) + corrupção silenciosa size-2 + vagas assimétricas. Todos corrigidos: semente=id da temporada (idempotência+auditoria, sem DDL); gate de torneios encerrados; porta de status; override restrito a permutação no grupo de sorteio + conservação por fronteira (Zod); `holderUserId` removido na Fase 1; `is_public` mantido (decisão do dono, texto ajustado); DISJUNÇÃO de cortes no motor (rebaixa antes, exclui caídos do acesso); `cortePonta` segue o destino realizado; FRONTEIRAS SIMÉTRICAS obrigatórias (decisão do dono — campo único no wizard). Varredura de 160 sementes nos cenários straddle/size-2.
- [x] 1.4.4 SEM DDL adicional nesta fase: a correção do sorteio reusa o id da temporada como semente (estável/auditável), dispensando a coluna `fluxo_seed` cogitada. A simetria de fronteiras é validada na aplicação (Zod + wizard), sem mudança de schema no banco.

## Fase 2 — Playoff de acesso + playout (bloco)

Decisão de produto (dono, 2026-06-13): suportar OS DOIS estilos por fronteira (`vagas` e `extra`) e leg format configurável por fronteira. Detalhe técnico no design §8. Gates por bloco: typecheck/lint/test/build + DDL via MCP (SQL antes) + validação ao vivo 390px/2 temas + revisão adversarial.

### 2.1 DDL (via MCP; espelhar em `supabase/schema.sql`)
- [x] 2.1.1 Pré-check read-only (mostrar SQL antes): confirmar que `league_boundaries` não tem as colunas novas; contar fronteiras não-`direto` existentes (devem ser 0 — Fase 1 só criou `direto`).
- [x] 2.1.2 `alter table league_boundaries` add `playoff_estilo text` (CHECK coerente com modo: `direto⇒null`, senão `in ('vagas','extra')`), `playoff_ida_e_volta boolean not null default false`, `playoff_tournament_id uuid references tournaments(id) on delete restrict` + índice único parcial (sentinela). Ver design §8.5.
- [x] 2.1.3 Estender `lock_division_tournament_reopen` (em `tournaments`) com 2º ramo barrando reabrir chave de `league_boundaries.playoff_tournament_id` de season `em_fluxo`/`encerrada` (design §8.7 camada 1). `create or replace` da função; trigger intacto. ALÉM do trigger, estender o GUARD da action `reabrirTorneio` (`tournaments.ts:523-527`) com ramo análogo em `league_boundaries` → retorna `erroPropriedade` (camada 2, UX consistente).
- [x] 2.1.4 RPC `montar_playoff(p_boundary_id uuid)` SECURITY DEFINER (design §8.6): posse transitiva, advisory lock, sentinela, cria `tournaments(formato='mata_mata', ida_e_volta, terceiro_lugar=false, por_nome/desempate/is_public herdados)`, insere slots da zona (passados como arg `p_competitor_ids uuid[]` na ordem de seeding) com degradação de `user_id`, guard de homogeneidade `por_nome`. `revoke ... from public, anon; grant ... to authenticated`.
- [x] 2.1.5 Aplicar via MCP (SQL completo antes; preferir branch Supabase). Verificar colunas/CHECK/índice/trigger/RPC.
- [x] 2.1.6 Espelhar TODA a DDL em `supabase/schema.sql` (mesmo estilo/comentários).
- [x] 2.1.7 Regenerar `database.types.ts` (3 colunas novas em `league_boundaries` + assinatura de `montar_playoff`). Typecheck verde.

### 2.2 Motor puro (zero IO, testado)
- [x] 2.2.1 `resultadoDaChave(partidas, { estilo, vagas, modo })` em `gerarChaveMataMata.ts`: varre as fases via `decidirConfronto` slot a slot; devolve `{ campeao, vicecampeao, sobreviventesAposRodada(f), eliminadosAteRodada(f), sobem|caem (Set), decidida }`. `decidida` = rodada `f` 100% encerrada no `vagas` (`f=log2(playoff_vagas/sobreviventes)`) / final decidida no `extra`. Exclui o slot de 3º lugar. Trata byes (sobrevive por vitória de confronto, não por "jogou"), W.O. e empate de agregado (barrado na persistência pelo trigger — §8.8; só fica indecidível se não-jogado).
- [x] 2.2.2 `semearPlayoffPorPosicao(competitorIdsOrdenados, slotPorCompetitor)` (puro, determinístico): bracket padrão (1×N, 2×N-1 espelhado) na ORDEM DE SLOT (pareamento `2i-1×2i` das fases seguintes cruza certo). Sem byes no `vagas` (chave completa 4/8/16/32); byes para os primeiros no `extra`. Recebe os ids JÁ na ordem de classificação. Teste de chave de 8 conferindo a árvore inteira.
- [x] 2.2.3 `movimentoEfetivo(fronteira)` EXPORTADO de `leaguePyramidSchema`: `{ sobeEf, caiEf }` dependente de **modo × estilo** — o `+1` do `extra` é de UM lado só (design §8.3): `playoff_acesso extra` ⇒ `(vagasAcesso+1, vagasRebaixamento)`; `playout extra` ⇒ `(vagasAcesso, vagasRebaixamento+1)`; `vagas`/`direto` ⇒ `(vagasAcesso, vagasRebaixamento)`. Fonte ÚNICA consumida por schema + wizard + actions.
- [x] 2.2.4 Estender `flowEngine.calcularPlanoFluxo`: ramo não-`direto` que recebe o conjunto sobe/cai vindo da chave. ORDEM UNIFICADA (§8.8): TODOS os rebaixamentos primeiro (diretos via `resolverZonaDeCorte` + playout via chave) no MESMO `caiDe`/`jaCaiu`; só então acessos (diretos + `playoff_acesso` via chave, excluindo `jaCaiu`). `resolvidoPor='playoff'`. Marca `permanece` para todo participante de chave sem desfecho (cobertura total `|itens|==Σtamanhos`). Assertion: sobe/cai da chave == `movimentoEfetivo` (divergência ⇒ erro). Tipo `ResolvidoPor` ganha `'playoff'`; `DivisaoFluxo` ganha o resultado da chave por fronteira.
- [x] 2.2.5 Testes puros: `resultadoDaChave` (campeão, sobreviventes/eliminados por rodada, byes, W.O., ida-e-volta); `semearPlayoffPorPosicao`; `calcularPlanoFluxo` com fronteira playoff (vagas e extra, acesso e playout) + mistura direto/playoff; conservação.

### 2.3 Actions
- [x] 2.3.1 Extrair de `iniciarMataMata` um helper server `gerarChaveSemeada(supabase, tournamentId, confrontos, idaEVolta)`: roda `gerarFaseInicial`, INSERT da 1ª fase ANTES do UPDATE de status, **PROMOVE `rascunho`→`ativo`** (senão a chave fica em rascunho e a página mostra re-sorteio), trata 23505. **SEM `participantes.sort()` nem `montarConfrontos*`** (destruiriam o seeding — §8.6). Recebe os `confrontos` já semeados.
- [x] 2.3.2 `montarPlayoffs(seasonId)`: gate (todas as divisões encerradas + posse). Para cada fronteira não-`direto`: resolve a ZONA pela classificação (top da inferior p/ acesso; bottom da superior p/ playout, conforme estilo+vagas), valida potência-de-2/zona-cabe, chama `montar_playoff(boundaryId, competitorIds_ordenados)` (RPC), e gera a chave via `gerarChaveSemeada`. **Retomada parcial**: se a sentinela existe mas NÃO há `matches WHERE rodada IS NOT NULL`, completa a chave (não pula cego — §8.6). Idempotente. Erros pt-BR.
- [x] 2.3.3 `calcularFluxoTemporada`: AMPLIAR o SELECT de fronteiras p/ `modo, playoff_estilo, playoff_vagas, playoff_tournament_id` (hoje cego — `leaguePyramid.ts:502`). 3 estados: sem chave ⇒ "Monte os playoffs"; chave `!decidida` ⇒ "Playoff pendente"; decidida ⇒ injeta o conjunto. `decidida` SÓ via `resultadoDaChave` (mesma fonte do gating da UI). Fronteiras `direto` seguem a Fase 1.
- [x] 2.3.4 `confirmarFluxoTemporada`: **EXIGE `status='encerrado'` em TODA chave de playoff** antes de montar a N+1 (§8.7 camada 3 — fecha janela de edição pós-confirmação); persiste `resolvido_por='playoff'`. `montarProximaTemporada` ADICIONA `playoff_estilo` e `playoff_ida_e_volta` ao `.select` (`leaguePyramid.ts:911`) E ao insert (`:931-935`) — senão o CHECK `estilo_coerente` estoura 23514 na N+1. ORDEM: DDL 2.1.2 + espelho + types ANTES de tocar `montarProximaTemporada`.

### 2.4 Schema/wizard (Zod + UI de criação)
- [x] 2.4.1 `fronteiraSchema`: `modo` vira `z.enum(['direto','playoff_acesso','playout'])`; campos `playoffEstilo?`, `playoffIdaEVolta`, `playoffVagas?`. Refines POR LADO/MODO (§8.10): estilo sse modo≠direto; `playoff_acesso vagas` ⇒ `playoff_vagas∈{4,8,16,32}` E `log2(vagas_acesso)∈ℤ` E `0<vagas_acesso<playoff_vagas`; `playout vagas` ⇒ `log2(playoff_vagas-vagas_rebaixamento)∈ℤ` E `0<sobreviventes<playoff_vagas`; `extra` ⇒ `playoff_vagas∈[2,32]`. **Zona cabe na divisão** (no `superRefine`, precisa do tamanho): acesso ⇒ zona ≤ tamanho inferior; playout ⇒ ≤ tamanho superior. Mensagem do `vagas` não-potência SUGERE `extra`.
- [x] 2.4.2 **REMOVER** o `.refine(vagasAcesso===vagasRebaixamento)` bruto de `fronteiraSchema:122`; MOVER a simetria para o `superRefine` do schema-mãe usando `movimentoEfetivo` (`sobeEf===caiEf`). Trocar movimento físico (`saem=sobeEf+caiEf`) e fechamento (efetivos) — `leaguePyramidSchema:315-345`. Client em LOCKSTEP (`erroConservacao`/`tamanhoFinal`). `derivarZonas`: zona de queda direta do `playoff_acesso extra` = `vagasRebaixamento` posições.
- [x] 2.4.3 `LeagueWizard` `PassoFronteiras`: `<select>` de modo por fronteira; quando playoff, expor estilo/leg/playoff_vagas e (no `extra`) vagas assimétricas; preservar campos novos no rebuild de `removerDivisao`; `montarInput` faz spread dos campos. Preset opcional `PRESET_CHAMPIONSHIP` (2 sobem + 1 na chave de 4, ida-e-volta na semi).
- [x] 2.4.4 `createCompetition` (action): persiste `modo`/`playoff_estilo`/`playoff_ida_e_volta`/`playoff_vagas` na fronteira. `montarProximaTemporada` já copia (2.3.4).

### 2.5 UI de jogo/visualização
- [x] 2.5.1 `PlayoffsPanel` (client novo) na página da temporada entre Divisões e Fim-de-temporada; aparece quando `todasEncerradas && !playoffsResolvidos`. Botão "Montar playoffs" (`montarPlayoffs`) + por chave: `BracketView` (RSC reúso) + link "Abrir chave" + `AvancarFaseButton` (reúso) **escondido após a rodada `f` no estilo `vagas`** (senão muda quem subiu). Padrão useTransition/toast/router.refresh.
- [x] 2.5.2 Fetcher `getPlayoffs(seasonId, userId)` (RSC): fronteiras não-`direto` + `playoff_tournament_id` + status + `PartidaDaChave[]` (reúso `getTournamentClassificacao`). Deriva `playoffsResolvidos` via `resultadoDaChave` (MESMA fonte do gate da action — §8.8). Indica os 3 estados (montar / pendente / resolvido).
- [x] 2.5.3 Página da temporada: gating — `mostrarFluxo = (todasEncerradas && playoffsResolvidos) || em_fluxo`; renderizar `PlayoffsPanel` quando pendente.
- [x] 2.5.4 `StandingsZonas`/`derivarZonas` ganham `playoffAcesso?`/`playoffRebaixamento?`; `StandingsTable` marca a zona de playoff com cor DISTINTA de `primary`(acesso)/`destructive`(queda)/`accent`(hover/sorteio) — âmbar/`gold` tracejada ou `warning`, contraste AA validado a 390px nos 2 temas + legenda de 3 itens. Aditivo.

### 2.6 Gate Fase 2
- [x] 2.6.1 `pnpm typecheck && pnpm lint && pnpm test && pnpm build` verdes (capturar exit REAL — `${PIPESTATUS[0]}`, não `echo` no pipe).
- [x] 2.6.2 Validação AO VIVO (chrome-devtools, logado, 390px, 2 temas): pirâmide com fronteira `playoff_acesso` estilo `vagas` + outra `playout` estilo `extra` ida-e-volta → montar → iniciar → placares → montar playoffs → jogar as chaves → calcular fluxo (com resultado da chave) → confirmar → N+1 conferida no banco (`resolvido_por='playoff'`, tamanhos conservados, chave congelada).
- [x] 2.6.3 Revisão adversarial por workflow (lentes paralelas + cético + síntese): conservação com efetivos, potência-de-2 do `vagas`, semântica do playout (`extra`=perdedor da final), freeze da chave, idempotência da montagem, RLS/locks, drift schema↔wizard. Corrigir HIGH+MEDIUM antes do commit.

## Fase 3 — Barragem cruzada (bloco)
- [ ] 3.1 `league_boundaries.modo` `'barragem_cruzada'` (X de d × Y de d+1): `montarConfrontosManual` com seeds explícitos + `gerarProximaFase`.
- [ ] 3.2 UI da barragem + integração ao plano de sobe/cai; testes; gate completo.

## Fase 4 — Promedios + página do competidor (bloco)
- [ ] 4.1 Base `promedios`: média plurianual lendo `league_division_entries` das N temporadas via `previous_season_id`.
- [ ] 4.2 Página do competidor `dashboard/ligas/competidor/[id]/`: histórico de temporadas/divisões/destinos; reúso de `league_competitors` + `league_division_entries`.
- [ ] 4.3 Testes; gate completo.

## Fase 5 — Ciclos alternativos + formato interno + custom (bloco)
- [ ] 5.1 Apertura/Clausura/split (meias-temporadas em `league_seasons`/`config_snapshot`).
- [ ] 5.2 Formato interno por divisão (grupos+mata-mata via `gerarFaseDeGrupos`/`gerarChaveMataMata`).
- [ ] 5.3 Desempate `custom` + `espanhol` (cadeia configurável + mini-tabela entre 3+ empatados). ALARGA os CHECKs `tournaments_desempate_valido`, `league_competitions_desempate_valido` e `league_division_seasons_desempate_valido` para incluir `'espanhol'`; adiciona `'espanhol'` ao `TiebreakerPreset`. Espelhar em `schema.sql`.
- [ ] 5.4 Testes; gate completo.

## Encerramento
- [ ] Z.1 Commit (pt-BR, Conventional Commits, sem coautoria de IA) + push.
- [ ] Z.2 `openspec archive add-ligas-piramide` após validação completa.
