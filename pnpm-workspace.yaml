ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
# Libera o postinstall do @sentry/cli (resolve o binário de plataforma p/ o
# upload de source maps no build do Vercel). pnpm v10 bloqueia build scripts por
# padrão; sem esta allowlist o upload poderia falhar no deploy.
onlyBuiltDependencies:
  - "@sentry/cli"
